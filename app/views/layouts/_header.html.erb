<div class="section navbar-container">
  <div class="navwidth">
      <ul class="navcontainer">
        <li class="navlogo-container mobile-no">
          <%= link_to image_tag("debatable-logo.png", :class => "navlogo"), root_path, {:method => 'GET'} %>
        </li>
        <li class="navlogo-container desktop-no">
          <%= link_to image_tag("logo.png", :class => "navlogo"), root_path, {:method => 'GET'} %>
        </li>
        <ul class="navlog">
          <% if session[:user_id] != nil %>

          <li class="navbar-link">
            <%= link_to 'New Firetalk', new_user_firetalk_path(@user), {:method => 'GET'} %>
          </li>
          <li class="navbar-link">
            <%= link_to('My Profile', @user) %>
          </li>
          <li class="navbar-link">
            <%= link_to 'Logout', logout_path, {:method => 'GET'} %>
          </li>
          <!--
          <div id="invitesContainer" data-url="<%= '/invites' %>">
            <% @invites.each do |invite| %>
            <%= render invite, :locals => {:invite => invite} %>
          <% end %>
        -->

        <% else %>
        <li class="navbar-link">
          <%= link_to "Signup", new_user_path, {:method => 'GET'} %>
        </li>
        <li class="navbar-link">
        <%= link_to 'Login', login_path, {:method => 'GET'} %>
        <% end %>
</li>
</ul>
</ul>
</div>
</div>
